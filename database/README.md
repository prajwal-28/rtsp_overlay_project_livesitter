# Database (MongoDB)

## Collections Structure
- **overlays**: Stores overlay configuration (id, position, size, content, z-index).
- **settings**: System settings (RTSP URL, Stream configuration).

## Setup
- MongoDB connection string required in backend configuration.
